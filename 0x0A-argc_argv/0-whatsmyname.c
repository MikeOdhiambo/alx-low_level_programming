#include <stdio.h>
#include "main.h"

/**
 * main - Prints the name of the program folowed by a new line
 * @argc: Number of command line arguments
 * @argv: Array of pointers to arguments
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
